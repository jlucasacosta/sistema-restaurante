# Backend — Configuracion
> Asume BACKEND_RULES.md.

- El diseno (theme) es estatico en shell/theme.ts: no necesita backend.
- Si se quieren settings por usuario: tabla settings (user_id, key, value).
